style.
  /* @import url("https://fonts.googleapis.com/css?family=Varela+Round"); */
  html,
  body {
  overflow-x: hidden;
  height: 100%;
  }
  body {
  background: #fff;
  padding: 0;
  margin: 0;
  }
  .header {
  display: block;
  margin: 0 auto;
  width: 60px !important;
  box-shadow: none;
  background-color: #1f1f1f;
  position: absolute;
  height: 60px !important;
  overflow: hidden;
  z-index: 10;
  border-radius: 50%;
  top: 30px;
  left: 30px;
  }
  .main {
  margin: 0 auto;
  display: block;
  height: 100%;
  }
  #sidebarMenu {
  height: max-content;
  position: fixed;
  left: 90px;
  width: 250px;
  padding-top: 00px;
  transform: translateX(-340px);
  transition: transform 250ms ease-in-out;
  background: #1f1f1f;
  border-radius: 2px;
  }
  .sidebarMenuInner {
  margin: 0;
  padding: 0;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  }
  .sidebarMenuInner a li {
  list-style: none;
  color: #fff;
  font-weight: bold;
  padding: 15px;
  cursor: pointer;
  border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }
  .sidebarMenuInner a li span {
  display: block;
  font-size: 14px;
  color: rgba(255, 255, 255, 0.5);
  }
  .sidebarMenuInner a {
  color: #fff;
  font-weight: bold;
  cursor: pointer;
  text-decoration: none;
  }
  input[type="checkbox"]:checked ~ #sidebarMenu {
  transform: translateX(0);
  }
  input[type="checkbox"] {
  transition: all 0.3s;
  box-sizing: border-box;
  display: none;
  }
  .sidebarIconToggle {
  transition: all 0.3s;
  box-sizing: border-box;
  cursor: pointer;
  position: absolute;
  z-index: 99;
  height: 100%;
  width: 100%;
  top: 21.5px;
  left: 19px;
  height: 22px;
  width: 22px;
  }
  .spinner {
  transition: all 0.3s;
  box-sizing: border-box;
  position: absolute;
  height: 3px;
  width: 100%;
  background-color: #fff;
  }
  .horizontal {
  transition: all 0.3s;
  box-sizing: border-box;
  position: relative;
  float: left;
  margin-top: 3px;
  }
  .diagonal.part-1 {
  position: relative;
  transition: all 0.3s;
  box-sizing: border-box;
  float: left;
  }
  .diagonal.part-2 {
  transition: all 0.3s;
  box-sizing: border-box;
  position: relative;
  float: left;
  margin-top: 3px;
  }
  input[type="checkbox"]:checked ~ .sidebarIconToggle > .horizontal {
  transition: all 0.3s;
  box-sizing: border-box;
  opacity: 0;
  }
  input[type="checkbox"]:checked ~ .sidebarIconToggle > .diagonal.part-1 {
  transition: all 0.3s;
  box-sizing: border-box;
  transform: rotate(135deg);
  margin-top: 8px;
  }
  input[type="checkbox"]:checked ~ .sidebarIconToggle > .diagonal.part-2 {
  transition: all 0.3s;
  box-sizing: border-box;
  transform: rotate(-135deg);
  margin-top: -9px;
  }
  .navigation-item {
  transition: ease-in-out 0.1s;
  }
  .navigation-item:hover {
  background: #209eed;
  }
  .default-cursor {
  cursor: default !important;
  }
  .jumbotron {
  padding: 2rem 1rem;
  margin-bottom: 2rem;
  background-color: #e9ecef;
  border-radius: 0.3rem;
  }

.header
  input#openSidebarMenu.openSidebarMenu(type='checkbox')
  label.sidebarIconToggle(for='openSidebarMenu')
    .spinner.diagonal.part-1
    .spinner.horizontal
    .spinner.diagonal.part-2
  #sidebarMenu
    ul.sidebarMenuInner
      a(href='/')
        li
          | Sebastian Productions&reg;
          span Freelance
      a(href='/articles')
        li.navigation-item All Articles
      a(href='/instagram')
        li.navigation-item Instagram
      a(href='/github')
        li.navigation-item Github
      a(href='/youtube')
        li.navigation-item YouTube
.main
  .row
    .col-md-12.jumbotron.text-center
      h1 Sebastian Productions&reg;
//- button(onclick='goBack()')
  label.sidebarIconToggle
    .spinner.diagonal.part-1
    .spinner.horizontal
    .spinner.diagonal.part-2